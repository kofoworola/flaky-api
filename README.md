# Consuming a flaky api in go

## How to run

Clone the project and `go run <project folder>` in the project folder. The images will be downloaded into the direcory the `go run` command is run from

## Req
- go 1.14+
